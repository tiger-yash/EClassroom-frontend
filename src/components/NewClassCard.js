import React from "react";
import { connect } from "react-redux";

const NewClassCard = props => {
  return <div>NewClassCard</div>;
};

const mapStateToProps = (state, props) => {
  return {};
};
export default connect(mapStateToProps, {})(NewClassCard);
