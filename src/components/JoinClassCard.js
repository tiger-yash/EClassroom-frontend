import React from "react";
import { connect } from "react-redux";

const JoinClassCard = props => {
  return <div>JoinClassCard</div>;
};

const mapStateToProps = (state, props) => {
  return {};
};
export default connect(mapStateToProps, {})(JoinClassCard);
